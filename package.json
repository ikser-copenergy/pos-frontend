{
  "name": "pos-frontend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "node -e \"process.exit(0)\"",
    "deploy:backend": "bash -lc 'cd ../pos-backend && git pull --rebase && npm ci && ( npm run test || { echo \"ERROR: Unit tests failed. Aborting deploy.\"; exit 1; } ) && npx prisma generate && npx prisma migrate deploy && npm run build && (pm2 describe pos-backend >/dev/null || pm2 start dist/index.js --name pos-backend) && pm2 restart pos-backend && pm2 save'",
    "deploy:frontend": "bash -lc 'git pull --rebase && npm ci && ( npm run test || { echo \"ERROR: Unit tests failed. Aborting deploy.\"; exit 1; } ) && npm run build && (pm2 describe pos-frontend >/dev/null || pm2 start npm --name pos-frontend -- run preview -- --host 0.0.0.0 --port 3003) && pm2 restart pos-frontend && pm2 save'",
    "deploy": "bash -lc 'set -e; cd ../pos-backend && git pull --rebase && npm ci && ( npm run test || { echo \"ERROR: Unit tests failed. Aborting deploy.\"; exit 1; } ) && npx prisma generate && npx prisma migrate deploy && npm run build && (pm2 describe pos-backend >/dev/null || pm2 start dist/index.js --name pos-backend) && pm2 restart pos-backend && pm2 save; cd - >/dev/null; git pull --rebase && npm ci && ( npm run test || { echo \"ERROR: Unit tests failed. Aborting deploy.\"; exit 1; } ) && npm run build && (pm2 describe pos-frontend >/dev/null || pm2 start npm --name pos-frontend -- run preview -- --host 0.0.0.0 --port 3003) && pm2 restart pos-frontend && pm2 save'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ikser-copenergy/pos-frontend.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ikser-copenergy/pos-frontend/issues"
  },
  "homepage": "https://github.com/ikser-copenergy/pos-frontend#readme",
  "description": "",
  "dependencies": {
    "pos-backend": "file:../pos-backend",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-image-crop": "^11.0.10",
    "react-router-dom": "^7.13.1",
    "react-select": "^5.10.2"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "@types/node": "^25.6.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.8",
    "tailwindcss": "^4.2.2",
    "typescript": "^5.9.3",
    "vite": "^8.0.0"
  }
}
